// if siempre va a devolver algo
// como println realmente no devuelve nada
// la expresion devuelve Unit (void)
if(47 % 3 > 0) println("No multiplo de 3")

val x = 3
val y = 10

val max = if(x > y) x else y

// devuelve anyVal porque no le hemos
// especificado que hacer en else
val maxX= if(x > y) true

val maxY = if (y>x) true

